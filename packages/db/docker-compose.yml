services:
  db:
    container_name: excali-plus
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: dev-amanydv
      POSTGRES_PASSWORD: excali-plus
      POSTGRES_DB: canvas-db
    volumes:
      - pgdata:/var/lib/postgresql
    ports:
      - "5432:5432"

volumes:
  pgdata: